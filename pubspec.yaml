name: junjao_bot
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  # UI + utilities
  cupertino_icons: ^1.0.8
  intl: ^0.19.0

  # Network + config
  http: ^1.2.0
  flutter_dotenv: ^5.1.0

  # Charts
  fl_chart: ^0.68.0

  # Local storage (stable on new Flutter)
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/.env
